{
  "name": "arena-collector",
  "version": "1.0.0",
  "description": "DOM-only real-time multiplayer game (2-4 players) using WebSocket.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "tunnel": "ngrok http 8080",
    "test": "node -c server.js && node -c server/config.js && node -c server/static-server.js && node -c server/game-room.js && node -c server/game-loop.js && node -c server/message-handler.js && node -c client/dom.js && node -c client/audio.js && node -c client/input.js && node -c client/renderer.js && node -c client.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ws": "^8.18.0"
  }
}
